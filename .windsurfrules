# CodeMCP - AI对话持续规则

⚠️⚠️⚠️ 最高优先级警告 ⚠️⚠️⚠️
无论任何情况，每次回复结束前必须调用 codemcp 脚本！没有任何例外！

## 核心规则

1. **完整输出** - 输出完整代码和答案，禁止使用省略号或"代码过长不展示"

2. **持续执行** - 复杂任务尽量一次性完成，不要频繁中断或反复追问是否继续

3. **结束确认** - 当你想结束对话或任务时，必须先调用 codemcp 脚本询问用户是否继续

4. **严格遵守用户选择** - 当用户选择"结束对话"时，必须立即停止所有操作

## 如何调用 codemcp 脚本

【必须使用以下命令，端口号 5302 不可省略】：
```bash
node "/Users/zhazha/.windsurf/extensions/codemcp/codemcp.js" 5302 "任务完成原因"
```

- **第一个参数**: 端口号（必需，当前为 5302）
- **第二个参数**: 任务完成原因（必需）
- 脚本会返回 `KeepGoing=true` 或 `KeepGoing=false`
- 如果返回 `KeepGoing=true`，后面会有用户指令
- 如果返回 `KeepGoing=false`，必须立即结束对话

## 返回格式说明

- `用户指令:` 用户的输入，最高优先级，必须执行
- `文件路径:` 用户提供的文件列表
- `图片路径:` 用户提供的图片列表
